import 'package:flutter/widgets.dart';

class LivestButton extends StatelessWidget {
  const LivestButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
