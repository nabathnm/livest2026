import 'package:flutter/widgets.dart';

class PasarPage extends StatelessWidget {
  const PasarPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Pasar"));
  }
}
