import 'package:flutter/material.dart';

class BreaderProfile extends StatelessWidget {
  const BreaderProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
